**Tag** — neutral chip/token with an optional color dot and dismiss ×. For filters & selections.

```jsx
<Tag color="var(--jade)">Production</Tag>
<Tag onRemove={()=>drop(id)}>research-agent</Tag>
```
