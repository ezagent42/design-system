**Card** — the floating white surface every layout sits on; radius 22, layered soft shadow.

```jsx
<Card>…</Card>
<Card elevation="raised" padding="sm">…</Card>
<Card interactive onClick={open}>…</Card>
```
- `elevation`: flat · raised · floating (default) · `padding`: none · sm · md · `interactive` lifts on hover
