**ColorPoints** — 点彩 scattered solid color-points; the system's signature texture.

```jsx
<ColorPoints />
<ColorPoints count={48} height={220} seed={12} />
<ColorPoints bare>{ <GlassPanel chip>overlay</GlassPanel> }</ColorPoints>
```
- Deterministic per `seed`. Use as a hero backdrop, empty-state texture, or section rhythm. Don't overuse — one per view.
