**Checkbox** — square; fills cobalt with a white check when on. Multi-select.

```jsx
<Checkbox checked={c} onChange={setC} label="Remember me" />
```

- `onChange(checked, event)`. Use for independent on/off choices in a list.
