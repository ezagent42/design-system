**Toggle** — on/off switch; track fills cobalt when on. For instant settings.

```jsx
<Toggle checked={on} onChange={setOn} label="Auto-run" />
<Toggle size="sm" checked={on} onChange={setOn} />
```

- `onChange(checked, event)` · `size`: `sm` · `md` (default)
